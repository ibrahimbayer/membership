{
  "name": "membership",
  "version": "1.0.0",
  "description": "Spring boot app using Mocha for integration tests",
  "dependencies": {
    "mocha": "^3.1.2",
    "supertest": "^2.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "mocha tests/index.js"
  }
}